---
last_verified: YYYY-MM-DD
applies_to: component or area name
---

# Verb-first title describing the fix, e.g. "Debugging failed briefing emails"

## Symptom

Paste the exact error text, log line, or observed behavior. This is what someone will paste into search — don't paraphrase it away.

## Root cause

What is actually happening, not just what the error message implies. If there were multiple candidate causes you ruled out along the way, note them briefly — it saves the next person from re-treading the same dead ends.

## Fix

The concrete steps, in order. Prefer commands and file paths over prose. If the fix is "change a setting in an external dashboard" (Supabase, Resend, Vercel), say exactly where.

## How to confirm it's fixed

What should you see once it's actually resolved — not just "it should work now," but a specific, checkable signal (a log entry, a status change, a value in a table).
